# CLAUDE.md — 장원 한자 학습앱 작업 지침

## 프로젝트
- 초4 자녀용 한자 학습·쪽지시험 대비 웹앱 (학원 교재: 장원급제Ⅲ 준4급, 본문편 11~26쪽)
- 사용 기기: 아이패드 + 애플펜슬 1세대, 갤럭시탭 + S펜 (브라우저)
- 배포: GitHub Pages (`main` 브랜치 루트). push 하면 1~2분 뒤 자동 반영
- 사용자: 부모(비개발자). 답변은 한국어, 개조식, 짧게. 여러 단계 작업은 단계마다 확인 후 진행

## 작업 규칙 (필수)
1. 수정 요청을 받으면 `index.html`을 고치고 아래 **점검**을 통과시킨 뒤 바로 커밋·푸시
   - `git add -A && git commit -m "한글로 변경 요약" && git push`
2. 파일은 `index.html` 한 개로 유지 (CSS·JS 내장). 빌드 도구 사용 금지
3. 외부 스크립트는 현재 쓰는 것만: hanzi-writer(jsdelivr), Google Fonts
4. 서버·Firebase 없음. 저장은 `localStorage` 키 `hanja_app_v3` (구조 바꾸면 기존 기록 이전 코드 작성)
5. 교재 데이터(`WORDS` 배열)는 교재 원문 기준. 사실 확인이 안 되는 내용은 추측으로 넣지 말고 사용자에게 "모름"으로 확인
6. 새 예시 문장에 실존 인물 실명 사용 금지

## 점검 (푸시 전)
- `node --check` 로 스크립트 문법 확인 (script 태그 내용 추출 후)
- 가능하면 Playwright로: 시작하기 → 배우기 → 5번 쓰기 → 훈·음 → 단어 → 누적 쪽지 → 부모 채점 흐름 오류 없음 확인
- 콘솔 오류 0개

## 구조 요약
- 데이터: `WORDS` → `DATA`(한자 192개, 단어당 연속 id 2개) → `WL`(단어 48개)
- 오늘의 학습(`daily`): 하루 5단어=10자
  - 글자마다: 배우기(3초 후 진행) → 5번 쓰기 → 훈·음 쓰기(펜 손글씨→글자 변환, 자동 채점)
  - 단어마다: 단어 익히기(3초) → 누적 쪽지(1번 단어부터 현재까지, 한자→한글 자동채점 / 한글→한자 부모채점)
- 5번 쓰기 규칙
  - 바탕 농도 `ALPHA=[0.26,0.13,0.05,0,0]` (4·5칸 없음)
  - 한 칸씩만 열림. 펜을 떼고 3초 지나면 완료 → 다음 칸
  - 최소 획 수(교재 획수×0.3) 미달이면 지우고 다시
- 펜 호환: 바탕 글씨는 canvas로 그림(텍스트 선택 방지), touchstart preventDefault, selectstart/contextmenu 차단, 손가락 무시(설정), S펜 버튼 눌린 입력 무시
- 부모 모드(기본 비밀번호 1234): 시험 범위 / 채점(누적 쪽지 쓰기 + 학원 쪽지시험) / 학습 현황 / 설정(진도 맞추기, 펜 전용, 비밀번호, 초기화)
- 카톡 리포트: `sendReport(date)` → 이미지+글, Web Share API(미지원 시 저장·복사 창)
- 이미지 기록은 최근 7일만 보관 (`KEEP_DAYS`)

## 할 일 후보
- 교재 27쪽 이후(셋째마당 등) 데이터 추가 여부 사용자 확인
- 실제 기기 테스트 결과 반영 (칸 크기, 펜 굵기, 3초 대기 체감)
